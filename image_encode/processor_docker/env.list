# Gitignore should ignore secrets in here so you can just put them on. If you
# want to commit changes, censor secrets and manually add with:
# git add -f image_encode/processor_docker/env.list
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-west-2
